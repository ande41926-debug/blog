---
tite: Data Visualization Walkthrough
date: '2026-06-03
tags: ['jupyterLite', 'data visualization', 'code', 'markdown']
draft: false
summary: Example of a markdown file with code blocks and syntax highlighting
---

## Data Visualization Workflow

## JupyterLite Notebook Code Cell Sections


Import dependencies 🐍

Most prjects rely on pre-written code libraries/modules whether written by the Notebook
author or by a third-party. When importing from libraries, it is common to use a aliases
such as pd is used below as a shorthand for pandas.

```python
# Dependaency Imports
import pandas as panda
imort matplotlib.pylot as plt
import json
from js import fetch
```

Helper function(s)

Helper funtion enable us to write clean human readable code.

```python
# Helper function which uses the browser's built-in fetch API
async def get_json(url):
    resp = await fetch(url)
    txt = await resp.text()
    return json.loads(txt)
```